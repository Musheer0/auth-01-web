import Settings from '@/components/auth01/ui/settings'
import React from 'react'

const page = () => {
  return (
   <section className='w-full h-screen flex items-center'>
    <Settings/>
   </section>
  )
}

export default page
